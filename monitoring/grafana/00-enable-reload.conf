[Service]
# 1 minute should be enough to allow reloads without timeouts even on loaded instances
TimeoutStartSec=1m
ExecReload=/usr/local/bin/reload-grafana.sh --password {{ pillar['credentials']['grafana']['admin_password'] }}
